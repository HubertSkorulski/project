package com.finalproject.service;

import com.finalproject.data.Mail;

public class EmailServiceTestSuite {

    Mail mail = new Mail(
            "hubertskorulski@gmail.com",
            "Test",
            "Test message"
    );

    //EmailService emailService = new EmailService(mail);
}
